(ns ^{:hoplon/page "index.html"} app.index
  (:require [hoplon.core :as h]
            [javelin.core :as j]
            {{#require}}
            {{{.}}}
            {{/require}}))

(j/defc services [{{#services}}
                             {{{.}}}
                             {{/services}}])

(uikit/app
  (uikit/dashboard :services services))

(-> (client/auth!))
